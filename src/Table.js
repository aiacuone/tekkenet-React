import React, {useEffect,useRef} from 'react'

export default function Table(props) {
    

    return (
        <div>
            {/* {console.log(props.input.value)}
            {console.log(props.input.name)} */}
            {props.secondLayerInput}
            {props.startNFinish1}
            {props.startNFinish2}
            {props.specificHeightValue}
            {props.specificFrame}
            {props.frameRangeValue1}
            {props.frameRangeValue2}
            Table
        </div>
    
    
    )
}
